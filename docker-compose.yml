services:
  mongodb:
    image: mongo:7
    container_name: coffee-shop-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=coffee-shop
    restart: unless-stopped

volumes:
  mongodb_data:
